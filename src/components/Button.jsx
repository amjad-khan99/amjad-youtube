

const Button = ({name}) => {
  return (
    <div>
        <button className=" bg-[#EEEDEB] w-full py-1 px-4 rounded-md font-semibold    ">
            {name}
        </button>
    </div>
  )
}

export default Button